#include <iostream>

using namespace std;

int main()
{
    int a,b; cout<<"a="; cin>>a; cout<<"b=";cin>>b;
    if(a==0) cout<<"PT vo no";
    else
        cout<<"x = "<<(float)(-b)/a;
    return 0;
}
