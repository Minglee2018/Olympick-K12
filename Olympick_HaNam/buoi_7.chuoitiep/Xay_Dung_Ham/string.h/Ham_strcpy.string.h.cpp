#include<bits\stdc++.h>
#define maxn 1001
 using namespace std; 
  int main() 
    { char a[maxn],b[maxn];
     gets(a);
     for(int i=0;i<strlen(a);i++){
     	b[i]=a[i];
	 }   
       puts(b);
	    return 0;
	}
