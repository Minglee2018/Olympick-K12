#include <bits/stdc++.h>

using namespace std;
void Tim_Kiem_Nhi_Phan( unsigned long long x, unsigned long long y)
{
    unsigned long long left=x,right=y,mid;
        while(left<=right)
        {
            mid=(left+right)/2;
            if()
        }



}


int main()
{
    int q; cin>>q;
    while(q--)
    {
        unsigned long long x,y;
        cin>>x>>y;
        chek(x,y);
    }
    return 0;
}
